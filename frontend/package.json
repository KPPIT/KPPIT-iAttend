{
  "name": "live-data-component",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel index.html --port 3001",
    "build": "parcel build index.html --dist-dir build --public-url ./"
  },
  "dependencies": {
    "streamlit-component-lib": "^1.4.0"
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  }
}
